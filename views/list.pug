extends layout.pug

block content

   ul.list-group
    each task in taskList
      li.list-group-item.task
        span :::: #{task.name}
        span :::: #{task.date}
        |
        span task.date